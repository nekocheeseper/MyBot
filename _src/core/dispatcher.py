
async def on_presence_update(before, after, bot) -> bool:
    return

async def on_message(message, bot) -> bool:
    return

async def on_reaction_add(reaction, user, bot) -> bool:
    return

async def on_voice_state_update(member, before, after, bot) -> bool:
    return
